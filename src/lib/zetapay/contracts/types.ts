export interface ContractAddresses {
  verifier: string;
  payroll: string;
  token: string;
}

export interface PayrollInitialization {
  employer: string;
  verifier: string;
  xlmToken: string;
  usdcToken: string;
}
